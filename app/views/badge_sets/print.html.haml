.raw.well
  = link_to t('forms.labels.back'), badge_sets_url, class: 'btn btn-large pull-right'
  
  %h4 Расположение листа бумаги будет
  .btn-group.print-switcher{data: {toggle: 'buttons-radio'}}
    %button.btn.btn-primary.active= t 'views.badge_sets.print.landscape'
    %button.btn= t 'views.badge_sets.print.portrait'
      

.badges
  - @badge_set.badges.each_with_index do |badge, index|
    = render partial: 'badges/badge', :locals => { :badge => badge } 
    
    -# if index % 2 == 0
    -# .page-brake
    -#    &nbsp;
    
